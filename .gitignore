# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

package-lock.json

# Environment files
.env.*

# Firebase
.firebase/

# Builder V2 types — synced from the server (canonical lives at
# aspect-agent-server/builder/types/index.ts). Generated by
# `npm run sync-types` (also hooked into postinstall / predev /
# prebuild). Don't commit the local copy — it'll drift.
src/builder/types/index.ts
